package MITP.team.backend.Repository;


import org.springframework.data.jpa.repository.JpaRepository;

public interface MedicalCaseData extends JpaRepository<MITP.team.backend.Model.MedicalCaseData, Long> {
}